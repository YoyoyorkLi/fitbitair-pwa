{
  "name": "pulse-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tap endpoint + PWA for Pulse. Point Vercel at this folder.",
  "scripts": {
    "test": "node lib/night.test.js && node lib/tag-auth.test.js && node lib/sync-endpoint.test.js && node lib/json-fixtures.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.58.0"
  },
  "engines": {
    "node": ">=20"
  }
}
